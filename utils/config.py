import os

# ── Gemini ─────────────────────────────────────────────────────
GEMINI_API_KEY = "AIzaSyCEf1DAHRhq108qkKqxaCAeN7phBxPfi1Y"
EMBED_MODEL = "gemini-embedding-001"
CHAT_MODEL = "gemini-2.5-flash"

# ── Chunker ────────────────────────────────────────────────────
# БЫЛО: 1000 символов — слова резались посередине, смысл терялся
# СТАЛО: 1500 символов + перекрытие 300 (20%) — меньше потерь на границах
CHUNK_SIZE    = 1500
CHUNK_OVERLAP = 300

# ── Vector Search ──────────────────────────────────────────────
# БЫЛО: 5 — мало для сложных вопросов
# СТАЛО: 8 — берём больше контекста, Gemini сам отфильтрует лишнее
TOP_K  = 8
DB_DIR = "./chroma_db"

# ── System prompt для RAG ──────────────────────────────────────
# БЫЛО: слабый промпт без указания языка и формата ответа
# СТАЛО: чёткие инструкции по языку, формату и поведению при отсутствии данных
SYSTEM_PROMPT = """Ты — умный ассистент районной администрации Нокис (Каракалпакстан).

ПРАВИЛА:
1. Отвечай на том же языке, на котором задан вопрос:
   - вопрос на каракалпакском → ответ на каракалпакском (кириллица)
   - вопрос на узбекском → ответ на узбекском
   - вопрос на русском → ответ на русском
2. Используй ТОЛЬКО информацию из предоставленного контекста. Не выдумывай.
3. Если в контексте нет точного ответа — скажи об этом прямо, не угадывай.
4. Отвечай кратко и по существу. Избегай воды и повторений.
5. Если вопрос требует числа, даты или имена — приводи их точно как в документе.
6. В конце ответа укажи источник: название файла из которого взята информация.

ФОРМАТ источника: "Источник: [имя файла]"
"""